name: Analisar ofertas e gerar alertas

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Instalar dependências
        run: pip install -r requirements.txt

      - name: Analisar ofertas
        run: python -m price_monitor.analysis.generate_alerts

      - name: Commitar banco atualizado
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add price_monitor/data/monitor.db
          git diff --cached --quiet && echo "Sem mudanças no banco." || git commit -m "Registra alertas gerados [skip ci]"
          git push
